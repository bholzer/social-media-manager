---
phase: 01-accounts-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/src/pages/AccountsPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to the accounts page and see a list of their connected social accounts"
    - "Each connected account displays its platform name and connection status (connected or expired)"
    - "When no accounts are connected, the page shows an empty state message"
    - "An account with null or future token_expires_at shows as Connected"
    - "An account with past token_expires_at shows as Expired"
  artifacts:
    - path: "src/client/src/pages/AccountsPage.tsx"
      provides: "Accounts list with status display"
      min_lines: 40
      contains: "useEffect"
  key_links:
    - from: "src/client/src/pages/AccountsPage.tsx"
      to: "/api/v1/social_accounts/"
      via: "api.get in useEffect"
      pattern: "api\\.get.*social_accounts"
---

<objective>
Complete the AccountsPage component to fetch and display connected social accounts with their status.

Purpose: Users need to see which social accounts are connected and whether their tokens are still valid, satisfying ACCT-01 and ACCT-05.
Output: A working AccountsPage.tsx that fetches from the API and renders account cards with connected/expired status badges.
</objective>

<execution_context>
@/Users/brennanholzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brennanholzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/client/src/pages/AccountsPage.tsx
@src/client/src/lib/api.ts
@src/client/src/pages/DashboardPage.tsx
@src/smm/schemas/social_account.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AccountsPage with API integration and status display</name>
  <files>src/client/src/pages/AccountsPage.tsx</files>
  <action>
Replace the stub in AccountsPage.tsx with a complete component that:

1. **Define a SocialAccount interface** matching the backend SocialAccountResponse schema:
   - `id: string`
   - `user_id: string`
   - `platform: string`
   - `platform_user_id: string`
   - `token_expires_at: string | null`

2. **Add a status helper function** `getAccountStatus(tokenExpiresAt: string | null): 'connected' | 'expired'`:
   - If `tokenExpiresAt` is null, return `'connected'` (null = long-lived token, NOT expired)
   - If `new Date(tokenExpiresAt) > new Date()`, return `'connected'`
   - Otherwise return `'expired'`

3. **Component state:** Three `useState` hooks:
   - `accounts: SocialAccount[]` initialized to `[]`
   - `loading: boolean` initialized to `true`
   - `error: string` initialized to `''`

4. **Data fetching:** `useEffect` with empty dependency array:
   - Call `api.get<SocialAccount[]>('/social_accounts/')`
   - On success, call `setAccounts(data)`
   - On error, call `setError(err instanceof Error ? err.message : 'Failed to load accounts')`
   - In `.finally()`, call `setLoading(false)`

5. **Render states:**
   - **Loading:** Show "Loading accounts..." text
   - **Error:** Show error message styled with red text
   - **Empty (accounts.length === 0):** Show a dashed-border box with "No connected accounts" heading and "Connect a social media account to start publishing." subtext
   - **Accounts list:** Render each account as a card (white background, rounded, shadow) showing:
     - Platform name (use `capitalize` CSS class on the platform string)
     - Platform user ID as secondary text (text-sm, text-gray-500)
     - Status badge: green pill ("Connected") using `bg-green-100 text-green-700`, or red pill ("Expired") using `bg-red-100 text-red-700`

Keep the existing page heading ("Social Accounts") and subtitle. Use `import { api } from '../lib/api'` (match the import style used in other pages -- check whether they use `@/lib/api` or relative paths and match).

Style with Tailwind CSS matching the existing app conventions (see DashboardPage and LoginPage for reference).
  </action>
  <verify>
    1. Run `cd /Users/brennanholzer/projects/personal/social-media-manager/src/client && npx tsc --noEmit` -- no TypeScript errors
    2. Visually inspect the file to confirm: useEffect with api.get call, status derivation function, loading/error/empty/list render states, status badge with green/red styling
  </verify>
  <done>
    - AccountsPage.tsx compiles without TypeScript errors
    - Component fetches from `/social_accounts/` on mount using api.get
    - Status correctly derived: null token_expires_at = connected, future = connected, past = expired
    - Empty state renders when no accounts exist
    - Each account card shows platform name, platform_user_id, and a colored status badge
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd src/client && npx tsc --noEmit` passes
2. Dev server runs: `cd src/client && npm run dev` starts without errors
3. Manual check: AccountsPage.tsx contains useEffect with api.get('/social_accounts/'), getAccountStatus function, loading/error/empty states, and account cards with status badges
</verification>

<success_criteria>
- AccountsPage fetches and displays connected social accounts from the API (ACCT-01)
- Each account shows platform name and connected/expired status badge (ACCT-05)
- Empty state displayed when no accounts are connected
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-accounts-page/01-01-SUMMARY.md`
</output>
