---
phase: 02-account-actions
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/client/src/pages/AccountsPage.tsx
autonomous: true

must_haves:
  truths:
    - "Each connected account shows a Disconnect button"
    - "Clicking Disconnect shows a confirmation dialog before removing the account"
    - "After confirming disconnect, the account disappears from the list without a page reload"
  artifacts:
    - path: "src/client/src/pages/AccountsPage.tsx"
      provides: "Disconnect button with confirmation for each account"
      contains: "handleDisconnect"
  key_links:
    - from: "src/client/src/pages/AccountsPage.tsx"
      to: "/api/v1/social-accounts/{id}"
      via: "api.delete()"
      pattern: "api\\.delete.*social-accounts"
---

<objective>
Add a disconnect button to each connected social account on AccountsPage, with confirmation dialog and optimistic removal from the list.

Purpose: Users need to remove accounts they no longer want connected.
Output: Updated AccountsPage with disconnect functionality.
</objective>

<execution_context>
@/Users/brennanholzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brennanholzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-account-actions/02-01-SUMMARY.md

Key source files to read before implementing:
@src/client/src/pages/AccountsPage.tsx
@src/client/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disconnect button with confirmation to AccountsPage</name>
  <files>src/client/src/pages/AccountsPage.tsx</files>
  <action>
Read the current `AccountsPage.tsx` (which will already have the Connect Facebook button from plan 02-01).

Add a disconnect button to each account list item. In the existing `accounts.map()` render, add a "Disconnect" button next to the status badge (in the right side of the flex row).

Implementation:
1. Add an `async function handleDisconnect(accountId: string)` that:
   - Shows `window.confirm('Are you sure you want to disconnect this account? This cannot be undone.')` — return early if cancelled
   - Calls `await api.delete(`/social-accounts/${accountId}`)`
   - On success, remove the account from state: `setAccounts(prev => prev.filter(a => a.id !== accountId))`
   - On error, set error state with the error message

2. In each account `<li>`, restructure the right side to include both the status badge AND a Disconnect button. Use a flex container with gap. The Disconnect button should be styled as a small, subtle danger button: `text-sm text-red-600 hover:text-red-800 hover:underline` (text-only style, not a filled button, to avoid visual noise).

3. Place the Disconnect button after the status badge in the flex row.

Do NOT use a custom modal — `window.confirm` is sufficient for v1.
  </action>
  <verify>
Run `cd src/client && npx tsc --noEmit` — no TypeScript errors.
Run `cd src/client && npx vite build` — build succeeds.
Confirm AccountsPage.tsx contains `handleDisconnect` function and `api.delete` call.
  </verify>
  <done>
Each account in the list shows a "Disconnect" button.
Clicking Disconnect shows a browser confirmation dialog.
Confirming the dialog calls `DELETE /api/v1/social-accounts/{id}` and removes the account from the displayed list.
Cancelling the dialog does nothing.
API errors are shown to the user.
  </done>
</task>

</tasks>

<verification>
1. `cd src/client && npx tsc --noEmit` passes
2. `cd src/client && npx vite build` succeeds
3. AccountsPage.tsx contains `handleDisconnect` that calls `api.delete(`/social-accounts/${accountId}`)`
4. AccountsPage.tsx contains `window.confirm` before the delete call
5. Account is removed from local state after successful delete (no page reload needed)
</verification>

<success_criteria>
- Each connected account shows a Disconnect button
- Clicking Disconnect triggers a confirmation dialog
- Confirming calls the DELETE endpoint and removes the account from the UI
- Cancelling does nothing
- Errors are displayed to the user
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-account-actions/02-02-SUMMARY.md`
</output>
